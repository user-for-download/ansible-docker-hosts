[[ -r "$HOME/.profile" ]] && emulate sh -c '. "$HOME/.profile"' 2>/dev/null || true
# >>> install.sh managed zprofile block >>>
[[ -r "$HOME/.local/etc/zprofile-banner.zsh" ]] && source "$HOME/.local/etc/zprofile-banner.zsh"
# <<< install.sh managed zprofile block <<<
